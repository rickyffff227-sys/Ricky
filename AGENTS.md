# 项目协作说明

开始处理本项目时先阅读 `PROJECT.md`，再按任务查阅 `DECISIONS.md`、`CHANGELOG.md` 和 `TODO.md`；以当前代码校验文档中的状态。README 主要写运行与部署。

收到被标记为“ChatGPT 给出的建议”的方案时，先根据真实代码与 `PROJECT.md` 评估适配性；如果影响既有架构或设计，先向用户说明影响。若 ChatGPT 与 Codex 的方案冲突，基于代码、需求和验证结果分析，不默认一方正确。

完成较大的功能、页面、设计或架构修改时，同步更新 `PROJECT.md`；重要取舍写入 `DECISIONS.md`，明显网站改动写入 `CHANGELOG.md`，待办同步到 `TODO.md`。尽量小步提交，commit message 说明目的。不要把未经本人确认的履历、成果或目标当成事实扩写。
